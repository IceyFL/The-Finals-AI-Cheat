<UserControl x:Class="Paster.UserController.AKeyChanger"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="42"
             d:DesignWidth="532"
             Height="42">
    <Grid>
        <Border Background="#FF191919"
                BorderBrush="#FF1E1E1E"
                BorderThickness="2"
                CornerRadius="5"
                Margin="0,0,0,10">
            <Grid>
                <Label x:Name="Title"
                       Foreground="White"
                       Content="Keyboard Activation Press"
                       FontFamily="/Paster2Trappy;component/Fonts/#Atkinson Hyperlegible"
                       VerticalContentAlignment="Center"
                       Padding="10,0,0,0"
                       FontSize="13"
                       Margin="0,0,115,0" />
                <Border Background="#30000000"
                        Width="100"
                        HorizontalAlignment="Right"
                        Margin="0,5,5,5"
                        CornerRadius="4"
                        BorderBrush="#8F000000"
                        BorderThickness="1">
                    <Label x:Name="KeyNotifier"
                           Foreground="White"
                           Content="Right Click"
                           FontFamily="/Paster2Trappy;component/Fonts/#Atkinson Hyperlegible"
                           VerticalContentAlignment="Center"
                           Padding="10,0"
                           FontSize="12"
                           Margin="0"
                           HorizontalContentAlignment="Center" />
                </Border>
                <Button x:Name="Reader"
                        Height="Auto"
                        Background="{x:Null}"
                        BorderBrush="{x:Null}"
                        Foreground="{x:Null}"></Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>